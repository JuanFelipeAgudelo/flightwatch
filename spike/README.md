# Phase 0 parser spike

Throwaway script, not part of the shipped app. Run against
`TRNP Assigments.zip` (owner's Downloads) extracted to `pdfs/` here.

    python parser.py

Outputs `parsed_results.json` (gitignored — contains real passenger PII:
names, phone numbers, hospital/embassy destinations). Findings write-up
lives in the 2026-09-16 session transcript, not in this repo.
